# 网站版（中心采集服务器）部署说明

网站版 `web_server.py` 是一个纯 Flask 应用，接收各机器 `agent.exe` 的上报，并提供网页仪表盘查看/搜索/导出资产。
**刻意不依赖任何 Windows 专用模块，可直接跑在 Linux 云服务器上。**

---

## 一、在服务器上跑起来

### 方式 A：gunicorn 生产运行（推荐，配合 Nginx 反代）
```bash
# 1. 上传本目录到服务器（如 /opt/cam）
cd /opt/cam
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt

# 2. 启动（监听 8000，可改 PORT 环境变量）
gunicorn web_server:app -b 0.0.0.0:8000 --workers 2 --timeout 60
```

### 方式 B：Flask 自带（临时/测试）
```bash
PORT=8000 python3 web_server.py
```

---

## 二、1Panel / Nginx 反代（让外网能访问）

1. 1Panel 里「网站 → 创建网站 → 反向代理」，目标填 `http://127.0.0.0:8000`（gunicorn 地址）。
2. 在「证书」里给域名申请/绑定 SSL，开启 HTTPS。
3. 安全组 / 防火墙放行对应端口。

---

## 三、让目标机把数据上报到这里

在桌面端「批量采集中心」：
1. 选 **互联网** 模式，填服务器地址 `http://你的域名` 或 `http://服务器IP:端口`；
2. 点【保存设置】→【生成部署包】；
3. 把 `agent_deploy` 文件夹拷到目标机，**双击 `agent.exe`** 即自动上报。

也可命令行直接上报：
```bat
agent.exe --server http://你的服务器:8000 --dept 部门 --user 使用人
```

---

## 四、接口速查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | `/`               | 网页仪表盘（搜索/导出） |
| POST | `/api/report`     | agent 上报接口，接收 JSON |
| GET  | `/api/machines`   | 返回全部机器 JSON（供「从服务器同步」） |
| GET  | `/export?fmt=xlsx`| 导出 Excel |
| GET  | `/export?fmt=csv` | 导出 CSV |

上报数据存放于 `数据存储/批量上报/*.json`，与桌面端 `server.py` 结构一致，可互通。
